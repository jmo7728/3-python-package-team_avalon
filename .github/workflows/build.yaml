name: Build and Test 

on: 
  pull_request:
    branches: [pipfile-experiment]
  push:
    tags: ["v*"]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    strategy:
      matrix:
        python-version: ["3.10", "3.11"]
    steps:
      - uses: actions/checkout@v4

      - name: Install Python, pipenv and Pipfile packages
        uses: kojoru/prepare-pipenv@v1
        with:
          python-version: ${{ matrix.python-version }}

          #installing requirements.txt
      - name: Install requirements.txt
        run: |
          if [ -f requirements.txt ]; then pipenv run pip install -r requirements.txt; fi

      - name: Turn on 'editable' mode
        run: |
          pipenv install -e .

      - name: Test with pytest
        run: |
          pipenv install pytest
          pipenv --venv
          pipenv run python -m pytest

  deliver:
    if: github.event_name != 'pull_request'
    needs: [build]
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4

      - name: Install Python, pipenv and Pipfile packages
        uses: kojoru/prepare-pipenv@v1

      - name: Build package
        run: |
          pipenv install build
          pipenv run python -m build .

        #change the following to the published one (not PyPI TEST)
      # - name: Publish to PyPI test server
      #   uses: pypa/gh-action-pypi-publish@release/v1
      #   with:
      #     password: ${{ secrets.TEST_PYPI_API_TOKEN }}
      #     repository-url: https://test.pypi.org/legacy/

  #ADDED to verify upload
  # verify-upload:
  #   if: github.event_name != 'pull_request'
  #   needs: [deliver]
  #   runs-on: ubuntu-latest
  #   timeout-minutes: 5
  #   steps:
  #     - name: Install from TestPyPI and import
  #       run: |
  #         python -m pip install --upgrade pip
  #         pip install --index-url https://test.pypi.org/simple/ --extra-index-url https://pypi.org/simple eatnyc
  #         python - <<'PY' import eatnyc print("Imported eatnyc OK. Version:", getattr(eatnyc, "__version__", "unknown")) PY